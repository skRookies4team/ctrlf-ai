# ========================================
# 환경 변수 설정 예시
# ========================================
# 이 파일을 .env로 복사하여 사용하세요
# 예: cp .env.example .env

# ========================================
# LLM 설정
# ========================================

# OpenAI 활성화 (dev-only)
# true로 설정 시 OpenAI API 사용
# false 또는 미설정 시 Mock LLM 사용
ENABLE_OPENAI=false

# OpenAI API 키
# ENABLE_OPENAI=true인 경우 필수
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI 모델
# 기본값: gpt-3.5-turbo
# 다른 옵션: gpt-4, gpt-4-turbo-preview 등
OPENAI_MODEL=gpt-3.5-turbo

# ========================================
# API 설정
# ========================================

# API 서버 주소 (Streamlit UI에서 사용)
# 기본값: http://localhost:8000
API_BASE_URL=http://localhost:8000

# ========================================
# 임베딩 설정
# ========================================

# 임베딩 제공자 선택
# dummy: 해시 기반 deterministic 임베딩 (기본값, GPU 불필요)
# qwen3: Qwen3/HuggingFace 임베딩 (langchain-community 필요)
# openai: OpenAI Embeddings (openai 패키지 필요)
EMBEDDING_PROVIDER=dummy

# 임베딩 차원 (기본: 384)
# 제공자에 따라 자동 조정됨
EMBEDDING_DIM=384

# Qwen3 모델 이름 (EMBEDDING_PROVIDER=qwen3 인 경우 사용)
# 기본: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# QWEN3_MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# ========================================
# 향후 확장 (예시)
# ========================================

# Upstage API 설정 (미구현)
# ENABLE_UPSTAGE=false
# UPSTAGE_API_KEY=your-upstage-api-key

# Local LLM 설정 (미구현)
# ENABLE_LOCAL_LLM=false
# LOCAL_LLM_MODEL_PATH=/path/to/model
