# FastAPI 및 서버
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# PDF 파싱
pypdf==4.0.1
pdfplumber==0.10.3  # 세희 파서에서 가져온 PDF 파서 (pypdf보다 강력)

# 데이터 모델
pydantic>=2.7.4

# 유틸리티
python-dateutil==2.8.2

# 벡터 스토어 (FAISS)
faiss-cpu>=1.8.0
numpy>=1.24.3,<2.0.0

# OCR (텍스트 추출 fallback)
pytesseract==0.3.10
pdf2image==1.16.3

# 테스트 (개발 환경)
pytest==7.4.3
pytest-cov==4.1.0

# Streamlit UI
streamlit==1.31.0
matplotlib==3.8.2
pandas==2.1.4

# LLM 통합 (선택사항 - dev-only)
# ENABLE_OPENAI=true 환경변수 설정 시 사용
# openai==1.12.0

# 샘플 PDF 생성 (선택사항)
# reportlab==4.0.7

# 다중 파일 형식 지원 (선택적 설치)
# HWP 파일 지원 (세희 파서) - 선택적
# pyhwp==0.1b9

# DOCX 파일 지원 - 선택적
# python-docx==1.1.0

# PPTX 파일 지원 - 선택적
# python-pptx==0.6.23

# 임베딩 모델 (선택적 설치)
# Qwen3/HuggingFace 임베딩 사용 시 필요 (EMBEDDING_PROVIDER=qwen3)
# langchain-huggingface>=1.0.1  # 권장 (최신)
# langchain-community>=0.4.1    # 대안 (deprecated)
# sentence-transformers>=2.3.1
# torch>=2.1.0  # CPU 버전
